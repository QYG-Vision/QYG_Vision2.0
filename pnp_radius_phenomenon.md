# 现象：更大运动半径下的 PnP 角度识别更准确

## 1. 透视形变（Perspective Distortion）与深度感知
当装甲板以更大的半径运动时，它在相机视野中会呈现出更明显的**视角变化（透视形变）**。
- **小半径运动**：装甲板几乎是在保持相近距离平移，或者角点间的相对尺寸变化不明显。此时四点在图像上的投影近似于仿射变换（Affine Transformation），丧失了深度的几何线索，导致 $R$（旋转）和 $t$（平移）发生强耦合，PnP 极容易陷入歧义解（翻转跳变）。
- **大半径运动**：装甲板的不同边缘离相机的深度差变大。透视投影下“近大远小”的特征非常显著（例如左侧灯条明显比右侧大）。这种非线性的透视畸变提供了强烈的 **深度梯度信息**，强制 PnP 算法打破仿射近似，使得能量曲面（Cost Surface）上的正确偏导方向变得极其陡峭且唯一。算法更容易锁定正确的旋转矩阵。

## 2. 像素量化误差（Quantization Error）的相对减小
- **图像分辨率限制**：相机传感器的像素是离散的。不管你在现实中转了多小的一个角度，如果投影在图像上的变化不到 1 个像素，算法就无法捕捉。
- **大半径的放大效应**：在更大的旋转半径下，装甲板在物理空间中的线速度和角位移引发了更大的图像像素位移。相同的像素检测噪声（如边缘模糊、二值化抖动带来的 1~2 像素误差）在总位移量的占比变小，即**信噪比（SNR）大幅提升**，使得 PnP 更能“看清”实际发生的变化。

## 3. 算法层面的解耦（Decoupling of Translation and Rotation）
- 在微小运动时，位置的微小平移 $dx$ 和绕目标微小旋转引发的偏移 $d\theta$ 在二维投影上看起来是**完全一样**的。PnP 优化器（如 Levenberg-Marquardt 等）极难区分这到底是平移还是旋转。
- 当运动半径够大，物体的旋转不再是微小量，几何结构的投影变化模式与纯平移的投影变化模式产生了本质差异。这就成功解耦了 $R$ 和 $t$，让解析器算出的角度变得更加纯粹和准确。

## 4. EKF 跟踪器（Tracker）的模型匹配（针对你的代码环境）
在你的代码里有扩展卡尔曼滤波（EKF）：
```cpp
x = [x, vx, y, vy, z, vz, a, w, r, l, h]
```
- EKF 内部状态包含旋转半径 `r`。当运动轨迹呈现明显的弧线（大半径）时，EKF 观测方程中的雅可比矩阵对状态量的偏导更加明确，滤波器能更快且更准确地收敛到真实的物理运动模型，从而平滑并修正了 PnP 传来的观测噪声，表现出“角度识别更准”的宏观现象。
